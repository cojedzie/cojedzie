import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import UiPin from "./UiPin.vue";

<Meta title="UI/Map/Pin"
      component={UiPin}
      argTypes={{
          variant: {
              options: ["outline", "filled", "filled-outline"],
              control: 'select',
          }
      }}
/>

export const Template = (args) => ({
    components: { UiPin },
    setup: () => {
        return { args }
    },
    template: "<ui-pin v-bind='args'><ui-icon icon='stop' /></ui-pin>"
})

# UiPin
<Canvas>
    <Story name="Outline" args={{ variant: 'outline' }}>{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Outline" />

<Canvas>
    <Story name="Filled" args={{ variant: 'filled' }}>{Template.bind({})}</Story>
</Canvas>

<Canvas>
    <Story name="Filled Outline" args={{ variant: 'filled-outline' }}>{Template.bind({})}</Story>
</Canvas>

